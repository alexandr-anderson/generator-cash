-- Механизм шаблонов рубрики убран: запись создавалась галочкой в редакторе, но при
-- генерации не читалась нигде — стиль серии держат цвета рубрики и carouselRecipe,
-- снятый с референсов. Вторая копия того же самого только обещала клиенту эффект,
-- которого не было.
DROP TABLE IF EXISTS `RubricTemplate`;
