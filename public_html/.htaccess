# postvmeste-node-proxy
# Timeweb document root: postvmeste/public_html
# Always proxy to the Next.js standalone process (including /).

<IfModule mod_rewrite.c>
RewriteEngine On
RewriteRule ^(.*)$ http://127.0.0.1:3001/$1 [P,L]
</IfModule>
