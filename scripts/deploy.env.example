# Copy to scripts/deploy.env on your computer or on the server.
# scripts/deploy.env is ignored by git.

DEPLOY_PATH=/home/c/cm149295/postvmeste
PUBLIC_HTML=/home/c/cm149295/postvmeste/public_html
GIT_BRANCH=main
GIT_REMOTE=origin
GIT_REPO_URL=https://github.com/alexandr-anderson/generator-cash.git

APP_NAME=postvmeste
# If port 3000 is already used by another app on the server, change to 3001
# and restart deploy (Apache proxy is regenerated automatically).
APP_PORT=3000
NODE_ENV=production

# Timeweb shared hosting often crashes native postinstall scripts.
NPM_CI_ARGS=--ignore-scripts

# Optional: uncomment if node/npm are not in default PATH on Timeweb.
# NODE_BIN=/home/c/cm149295/.nvm/versions/node/v22.14.0/bin/node
# NPM_BIN=/home/c/cm149295/.nvm/versions/node/v22.14.0/bin/npm
# PM2_BIN=/home/c/cm149295/postvmeste/node_modules/.bin/pm2

# Local-only settings for npm run deploy
SSH_HOST=vh470.timeweb.ru
SSH_USER=cm149295
SSH_PORT=22

# Optional SSH key path for local deploy script.
# SSH_IDENTITY_FILE=~/.ssh/id_ed25519

# GitHub Actions secrets (Settings -> Secrets and variables -> Actions):
# SSH_PRIVATE_KEY  - private key with access to Timeweb SSH
# SSH_HOST           - vh470.timeweb.ru
# SSH_USER           - cm149295
# SSH_PORT           - optional, default 22
